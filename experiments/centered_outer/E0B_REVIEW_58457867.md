# E0b：集群验收及本地原始产物复核通过

## 已收到的证据

用户回传了 `e0b-58457867-20260917-021226` 的末段终端输出及完整 summary。
集群路径：`/pscratch/sd/s/syfan/Pier/local/centered_outer/e0b-58457867-20260917-021226`。
summary 完成时间为 `2026-09-17T09:15:21.770642+00:00`，记录：

- `status: passed`、`stage: E0b`、`GPU_executed: true`、`errors: []`；
- 实际入口 `pretrain_gpt.py`，world size 4；
- 八阶段 tp1-s1、tp1-s2、tp1-s4、tp1-host、split、resume、tp2-s2、dp2-s2；
- s2、s4、host、resume 对 s1 的 16 个 per-rank 轨迹比较全部逐位一致；
- 最后一阶段 `[E0b 8/8] DONE dp2-s2 (exit=0)`；
- `performance_result: false`。

完整产物现已通过 Git 回传：75 个文件、773864 bytes，包括 32 份 rank 报告、
32 份启动记录、8 份阶段日志及 manifest/node/summary。只读重新执行验收得到
passed、零错误；除新的检查时间戳外，结果与原始 summary 完全一致。
390 个执行源码哈希逐一匹配运行提交 `025f5139a38a18462057e50e6e63f480c49cdeda`。
本地复核提交为 `576c52067d8fef45df3e842bd6de4492e3f88fc2`，发生在后续 Qwen
入口修改之前；后续复现实验源码以运行提交为准，不将新代码与历史 manifest 混比。
每个原始文件的 SHA256/大小见 [机器可读审查记录](E0B_AUDIT_58457867.json)。
没有重写原始 summary、manifest 或任何 rank 报告。

节点为 nid001160，4×A100-SXM4-40GB，任意 GPU pair 的拓扑记录为 NV4。
PyTorch 2.6.0+cu124、CUDA build 12.4、NCCL 2.21.5；没有测链路有效带宽。
恢复 receipt 的 iteration=5、successful=4、consumed samples=40；实际恢复后的
第一个 forward 另做一次提交检查，因此 resume 的 consumer_checks=4，连续运行是 3。

## 本轮正确性范围

按 E0b 固定配方，真实两层小 GPT 使用 BF16 model、FP32 masters/outer state、
AdamW、dropout、MockGPT 数据和单 slot executor；一节点四 GPU。
完整轨迹为 11 attempted steps，其中第 3 步全局 skip，10 successful steps，
outer 位于 attempted 4/7/10，并保留第 11 步检查下一次 forward。
split 在第 5 步保存，resume 用新进程恢复后运行至第 11 步。

当前汇总代码在报告 passed 前要求：实际 CUDA/NCCL、完整启动参数、rank/group
映射、成功步计数、skip 保持状态、固定图 oracle、master/model/inner-state/loss
轨迹、恢复文件 hash/size receipt、下一个 forward 和执行源码 hash 均符合合同。
据此接受这次**集群 E0b gate 及本地归档复核通过**。
TP2 和 inner-DP2 检查各自的对应坐标合同，不要求其轨迹等于 TP1。

## 退出警告

四个 rank 在退出时提示未显式调用 `destroy_process_group()`。本次末阶段退出码为
0，最终 summary passed，未出现该阶段退出挂起或训练失败。
它表明退出清理尚待改进，不是数值校验失败；也不据此宣称不存在资源清理风险。
[PyTorch 2.6 文档](https://docs.pytorch.org/docs/2.6/distributed.html#shutdown)
要求在通信完成后由各训练进程显式销毁进程组，以确定顺序释放 NCCL 通信器。
后续启动器维护时处理，不为此单独重复本轮正确性实验。

## 归档与下一阶段

该目录下 JSON/log 已通过普通 Git 回传并逐份核对。
checkpoint 二进制仍保留集群；这一轮回传无需 Git LFS，也无需提交新作业。

通过范围不包括 Qwen 真权重/真实数据配方、distributed optimizer、多 slot
pipeline、PP/CP/MoE、跨拓扑恢复、强基线性能、GPU 峰值或物理 wire traffic。
因此不能把 E0b 当作完整 E0，更不能填写吞吐/性能比较表。

原生 Qwen 训练入口及 weights-only 初始化、tokenizer/data 文件身份检查已接入并
完成 CPU 检查；下一步补真权重 GPU 对照和实际语料配方，再实现同工作负载的强
基线与完整周期计量，进入 E1/E2。下一份 [E0c GPU 脚本](../qwen/E0C_HANDOFF.md)
现已准备并通过本地 CPU 回归，申请 1 节点 4 卡 1 小时；尚未取得 GPU 结果。
