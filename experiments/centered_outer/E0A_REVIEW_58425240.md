# E0a 实验进度审查 — 作业 58425240

## 结论

E0a 的四卡组件级正确性报告通过，原始文本证据已回传并逐项核对。
当前处于“GPU 组件已验证，准备接通完整训练路径”的阶段。
完整 E0、真实 Qwen 工作负载及论文性能比较尚未完成。

必须区分两条已验证的路径：

1. Megatron gate 调用现有 `centered_allreduce_update`，验证 MyDDP、普通
   BF16/FP32 optimizer 的提交、归一化、状态保留和局部恢复。
2. 新 `CenteredExecutor` 在独立 CUDA/NCCL verifier 中运行，验证固定图执行。

`megatron/training/training.py` 仍调用旧 helper，没有调用新 executor。
两条路径各自通过，并不意味着新协议已经在真实 Megatron 训练循环中端到端通过。

## 收到的证据及环境

- 结果提交：`e3a702bc1e2f60f97b6e4a8047f9eb044d1ee1bd`。
- 作业记录的运行源码提交：`0f41ecdf4d3a20dd0664bdc88c34778e62c433bd`；
  运行时 `git status` 输出为空，但忽略文件不会出现在这个状态中。
- 原始目录：`local/centered_outer/e0-58425240/`，15 个文件、309149 bytes
  （约 302 KiB），含 12 个 JSON 和 3 个阶段日志。
- 根目录的 `pier-e0-58425240.out` 已在先前提交中收到。
- 一节点四张 `NVIDIA A100-SXM4-40GB`，节点 `nid001808`。
- PyTorch `2.6.0+cu124`，PyTorch CUDA build `12.4`，NCCL `2.21.5`，Python `3.11.14`。
- 驱动 `580.159.04`；nvidia-smi 显示的 CUDA 13.2 是驱动支持信息，不能覆盖
  本次 PyTorch 所记录的 CUDA 12.4。
- `nvidia-smi topo -m` 显示任意 GPU pair 为 NV4；本次没有测有效带宽或跨节点流量。

普通 Git 足够保存这轮文本证据，不需要 Git LFS。

## 核对结果

| 项目 | 结果与覆盖范围 |
|---|---|
| 原始汇总 | `passed`、4 ranks、GPU executed、errors 为空 |
| Megatron DP1 / DP2 | 两阶段各 4 个 rank 报告完整；GPU、软件版本和 hostname 一致 |
| 梯度归一化 | 每报告 4 组合：SUM/AVG × 旧 full-DP divisor/inner average；inner 与 lazy-start 检查通过 |
| outer 更新 | 每报告 4 组合：shard × CPU storage；master/model/下一次 forward、moments 和局部 optimizer restore 通过 |
| 缺少提交负对照 | 每报告均观察到缺少 master→model copy 导致的旧模型值，随后提交检查通过 |
| 固定图 protocol | 48 个全局配置，192 条 rank 记录；3 cohort × 4 长度 × 2 tile × 2 storage tier |
| 连续边界与缓冲区 | 每配置 3 个 outer boundaries；状态/model bits、固定 storage 和正数 guard operation count 通过 |
| toy training | 3 个全局配置，12 条 rank 记录；每配置 7 个 inner steps、3 个 outer boundaries；后续 forward、inner moments、drained-boundary executor restore 通过 |
| 不安全配置拒绝 | 每 rank 拒绝 3 个配置 |
| 分片与 rank map | DP1 和 DP2 的 inner/outer group membership 均核对一致；DP2 使用复制式普通 optimizer |
| 显式内存及逻辑 bytes | 逐条复核 192 条 allocation/payload：workspace、R/M 分片字节数、4 阶段 payload 及 padded size 均符合公式 |

以上配置数不是独立性能重复次数。显式 tensor storage 与逻辑 payload 不代表
GPU 总峰值、allocator/NCCL 内部占用或物理 NIC/NVLink wire bytes。

## 源码留档限制

manifest 记录 371 个源码 hash：

- 本地工作树存在的 358 个全部一致，无内容差异。
- 12 个 `megatron/legacy/data/*.py` 被宽泛的 `data/` 规则忽略，未进入 snapshot 分支。
  已逐文件核对旧 main 提交 `e0621757e9daadf6524b3d0519bb6bdeb47019c6`，12 个 hash 全部吻合。
- `megatron/training/training_legacy.py` 被显式忽略，目前只拿到 hash，未找到对应内容。
  当前 E0 harness 与 Megatron 源码的静态引用检索没有发现它的引用；这不能代替运行时 import trace。

只读调用当前 `summarize()` 时，唯一报错是源码 hash 集合不完整；所有逐 rank、
protocol、矩阵完整性和执行器 hash 校验都通过。这是归档/工作树差异，
没有发现本次算法结果失败，也没有修改原始 summary、manifest 或降低验证标准。
完整源码快照复现仍需补齐该 legacy 文件，并处理源码被数据目录规则误忽略的问题。

## 与 draft E0–E5 的对应关系

| 阶段 | 目前进度 | 还缺什么 |
|---|---|---|
| E0a 组件正确性 | 本次范围内通过 | 完整源码留档还缺上述一个 legacy 文件 |
| 完整 E0 | 部分完成 | 生产坐标 adapter、真实 pretrain_gpt 接入、成功步/skip 调度、per-learner 完整状态恢复、真实模型及并发 copy/slot 检查 |
| E1 强基线吞吐 | 未执行当前稿配方 | Qwen/数据配方、G/R/O/S/T/P/W arms、公平调优、完整周期计时与 paired runs |
| E2 显存/数据移动 | 仅小 fixture 的显式 workspace/逻辑 bytes 已验证 | 真实负载 GPU/host/pinned 峰值、allocator/device accounting、物理链路流量 |
| E3 消融 | 未执行当前稿配方 | gather-R、cohort、pipeline、recompute 等受控比较；E0 的 cohort 正确性覆盖不能当性能消融 |
| E4 覆盖与扩展 | 未执行当前稿配方 | Qwen 各规模、32→64→128 GPU、GH200/A100-80、distributed optimizer ownership |
| E5 数值诊断及结论边界 | 人工输入/toy 路径已有证据 | 真实模型 FP32 状态的不同图误差与短窗口诊断；不声称最终质量或 full-pretraining 结论 |

正文 correctness 表的 10 个格子不能整体标成完成：fixed-graph outer state 和
next consumer 已有 toy/GPU 证据，其他生产、alias/skip/并发、不同图数值项仍需补充。
性能表 7 arms × 6 指标的 42 格，目前没有可由这轮实验填入的性能数据。
历史 `.out` 文件不自动成为当前新方法、语义及工作负载下的实验结果。

## 建议的下一步

1. 完成 E0b 的最小生产接入：参数/optimizer master 到规范坐标的映射、按对应
   TP/inner 坐标建 outer group、把新 executor 接入实际 pretrain_gpt，正确提交模型。
   第一版以普通 optimizer、TP1、inner-DP1、单 slot 为闭环，随后扩到必要布局。
2. 同时统一 attempted/successful step 计数、跨 learner 的 skip 决策和完整状态恢复。
   当前 outer 触发仍基于 `iteration % outer_sync_interval`，不满足 draft 的成功步合同。
3. 四卡小 GPT 端到端验证：连续运行与保存/恢复对照、注入 skip、跨至少 3 个 outer
   边界，并检查 outer 后的下一次 forward。全部计入每次最多 500 attempted steps。
4. 接 Qwen 配置/权重转换和固定数据配方，过真实模型 E0 后，才开始 E1 的强基线性能比较。

当前主要工程工作是完整训练路径的接入与恢复语义，不能只靠放大 GPU 数量推进。
